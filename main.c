#include "prepare.h"

//we have to put fresh doughnut into boxes from doughnut stockpile and carry those boxes to truck

int main(void) 
{
	go_to_work(); // go_to_work 내부에서 duty함수가 호출된다. go_to_work함수에는 문제가 없다.
	return (0);
}